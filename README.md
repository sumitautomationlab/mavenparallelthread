# cafe_orion_migration

# This framework to run parallel browser and used Hybrid framework i.e. conbination of TDD and Keywork driven

# install latest jdk, maven, import all the dependencies run testng.xml